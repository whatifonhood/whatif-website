## What this changes

<!-- One or two sentences. If it fixes an issue, add "Fixes #123". -->

## Why

<!-- What was wrong, or what this makes possible. If it is a bug fix, what was
     the broken behaviour and how do you know it is fixed now? -->

## Checks

- [ ] `npm run check` passes (typecheck, lint, format, build)
- [ ] `npm test` passes
- [ ] Copy changes are in all four languages, or the key is not user-facing
- [ ] No inline `style` attributes (the CSP drops them silently)
- [ ] Generated files under `src/config/` were regenerated with their script,
      not edited by hand

## Anything else

<!-- Screenshots for visual changes are very welcome — before and after, and a
     narrow width if it touches layout. Most visitors are on a phone. -->
