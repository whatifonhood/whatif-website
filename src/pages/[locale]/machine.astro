---
import type { GetStaticPaths } from 'astro';
import Machine from '../../components/pages/Machine.astro';
import { LOCALES, type Locale } from '../../config/site.ts';

/**
 * Machine in every language except English, which is served from the root.
 *
 * The body lives in the component so the two routes cannot drift apart.
 */
export const getStaticPaths = (() =>
  LOCALES.filter((locale) => locale !== 'en').map((locale) => ({
    params: { locale },
    props: { locale },
  }))) satisfies GetStaticPaths;

const { locale } = Astro.props as { locale: Locale };
---

<Machine locale={locale} />
